import CustomLayout from '@/Layouts/CustomLayout';
import React from 'react';

const CustomContact = () => {
    return (
        <CustomLayout>
            <p>this is custom contact</p>
        </CustomLayout>
    );
};

export default CustomContact;
