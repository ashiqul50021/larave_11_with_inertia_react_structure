import CustomLayout from '@/Layouts/CustomLayout';
import React from 'react';

const CustomHome = () => {
    return (
        <CustomLayout>
            <p>this is custom home.</p>
        </CustomLayout>
    );
};

export default CustomHome;
